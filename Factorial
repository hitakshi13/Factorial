# include <iostream>
using namespace std;
int fact (int n)
{
	int answer=1;
	for (int i=1;i<=n;i++)
	{
		answer*=i;
	}
	return answer;
}
int main ()
{
	int n;
	cout<<"Enter a number: ";
	cin>>n;
	cout<<"The factorial of "<<n<<" is "<<fact(n);
	return 0;
}
